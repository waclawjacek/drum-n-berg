<?php
/*
 * The `drum-n-berg/track` block does not define its own rendering logic.
 *
 * It is rendered via the `drum-n-berg/drum-machine` block's JS
 * as it cannot exist outside of a `drum-n-berg/drum-machine`.
 */
